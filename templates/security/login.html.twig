{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
<section class="min-h-screen flex items-center justify-center bg-gradient-to-br from-blue-50 to-green-50 p-4">

    <div class="w-full max-w-md bg-white rounded-2xl shadow-xl overflow-hidden">

        {# Header #}
        <div class="bg-gradient-to-r from-blue-500 to-green-500 text-white text-center p-8">
            <h1 class="text-3xl font-bold text-gray-900">Connexion</h1>
            <p class="text-sm mt-2 opacity-90 text-gray-900">
                Accédez à votre espace personnel
            </p>
        </div>

        <div class="p-8">

            {# Already logged in notice #}
            {% if app.user %}
                <div class="mb-6 p-4 bg-green-100 border border-green-300 rounded-lg text-green-800">
                    Connecté en tant que {{ app.user.userIdentifier }}
                    —
                    <a href="{{ path('app_logout') }}" class="underline">
                        Se déconnecter
                    </a>
                </div>
            {% endif %}

            {# Login error message #}
            {% if error %}
                <div class="mb-6 p-4 bg-red-100 border border-red-300 rounded-lg text-red-700">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}

            {# Success flash messages (ex: email verification sent) #}
            {% for message in app.flashes('success') %}
                <div class="mb-6 p-4 bg-green-100 border border-green-300 rounded-lg text-green-800">
                    {{ message }}
                </div>
            {% endfor %}


            <form method="post" class="space-y-6">

                {# Email field #}
                <div>
                    <label for="username"
                           class="block text-sm font-medium text-gray-700 mb-2">
                        Adresse email
                    </label>

                    <input
                        type="email"
                        value="{{ last_username }}"
                        name="_username"
                        id="username"
                        class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                        autocomplete="email"
                        required
                        autofocus
                    >
                </div>


                {# Password field #}
                <div>
                    <label for="password"
                           class="block text-sm font-medium text-gray-700 mb-2">
                        Mot de passe
                    </label>

                    <input
                        type="password"
                        name="_password"
                        id="password"
                        class="w-full px-4 py-3 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                        autocomplete="current-password"
                        required
                    >
                </div>


                {# CSRF protection #}
                <input
                    type="hidden"
                    name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}"
                >


                {# Remember me checkbox #}
                <div class="flex items-center">
                    <input
                        type="checkbox"
                        name="_remember_me"
                        id="remember_me"
                        class="mr-2"
                    >

                    <label for="remember_me"
                           class="text-sm text-gray-600">
                        Se souvenir de moi
                    </label>
                </div>


                {# Submit button #}
                <button
                    type="submit"
                    class="w-full bg-gradient-to-r from-blue-500 to-green-500 text-black font-semibold py-3 rounded-lg shadow-md hover:opacity-90 transition"
                >
                    Se connecter
                </button>

            </form>


            {# Divider #}
            <div class="my-6 text-center text-sm text-gray-500">
                Pas encore inscrit ?
            </div>


            {# Register link #}
            <a
                href="{{ path('app_register') }}"
                class="block text-center border border-blue-500 text-blue-600 py-3 rounded-lg hover:bg-blue-50 transition"
            >
                Créer un compte
            </a>

        </div>
    </div>

</section>
{% endblock %}